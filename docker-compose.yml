version: "2.1"

services:
    app:
        build: .
        environment:
            APPLICATION_ENV: dev
        volumes:
            - ./:/var/www

    composer:
        image: composer:1.3
        volumes:
            - .:/app
