language: php

sudo: false

cache:
    directories:
        - $HOME/.composer/cache

php:
    - 7.1

install:
    - travis_retry composer install --prefer-dist --no-interaction

script:
    - vendor/bin/phpunit
